language: elixir
elixir:
  - 1.2.6
otp_release:
  - 19.0

install:
  - mix local.hex --force
  - mix local.rebar --force
  - mix deps.get

script:
  - mix test
notifications:
  recipients:
    - kelostrada@gmail.com